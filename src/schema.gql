# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  accessToken: String!
  user: UserResponse!
}

input CreateListingInput {
  description: String!
  latitude: Float!
  longitude: Float!
  quantity: String!
  title: String!
}

enum ListingStatus {
  AVAILABLE
  CLAIMED
  COMPLETED
  EXPIRED
}

type ListingType {
  description: String!
  id: String!
  quantity: String!
  status: ListingStatus!
  title: String!
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  acceptPickup(listingId: String!): PickupType!
  createListing(input: CreateListingInput!): ListingType!
  login(input: LoginInput!): AuthResponse!
  register(input: RegisterInput!): AuthResponse!
  updatePickupStatus(pickupId: String!, status: PickupStatus!): PickupType!
}

enum PickupStatus {
  ASSIGNED
  CANCELLED
  DELIVERED
  IN_TRANSIT
}

type PickupType {
  id: String!
  status: PickupStatus!
}

type Query {
  healthCheck: String!
  listings: [ListingType!]!
  nearbyListings(latitude: Float!, longitude: Float!, radiusKm: Float!): [ListingType!]!
}

input RegisterInput {
  email: String!
  name: String!
  password: String!
  role: UserRole!
}

type UserResponse {
  email: String!
  id: String!
  name: String!
  role: UserRole!
}

enum UserRole {
  DONOR
  NGO
  VOLUNTEER
}